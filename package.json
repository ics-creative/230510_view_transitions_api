{
  "name": "vta-thumbs",
  "private": true,
  "devDependencies": {
    "prettier": "^3.3.1"
  },
  "scripts": {
    "format": "prettier --write {vta-thumbs,vta-mpa,vta-example,vta-transition}/**/*.{html,js,css}"
  },
  "workspaces": [
    "vta-thumbs",
    "vta-modal",
    "vta-transition"
  ]
}
